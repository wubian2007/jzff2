// ============================================
// ⚙️ 跳转链接配置 - 在这里修改所有按钮的跳转地址
// ============================================
const TARGET_URL = 'https://www.baidu.com';

// 导出配置
if (typeof module !== 'undefined' && module.exports) {
    module.exports = { TARGET_URL };
}

